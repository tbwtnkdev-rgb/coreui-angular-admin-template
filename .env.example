# Copy to .env and fill in. .env is gitignored; this file is the contract.
#
# POSTGRES_PASSWORD has no default anywhere in the stack. Compose refuses to
# start without it, which is the intended behaviour: a default password is one
# that reaches production.

POSTGRES_DB=dashboard
POSTGRES_USER=dashboard
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
